---
phase: 14-client-refinements
plan: 14-01
title: Client Refinements, Star Ratings, Catalog Sorting & Mobile Fixes
type: feature
wave: 1
depends_on: []
---

# Plan 14-01: Client Refinements & Bug Fixes

## Objective
Implement guest star ratings separated from authenticated written reviews, add comprehensive catalog sorting, replace landing page and about page images with custom royalty-free luxury assets, update 30% to 40% concentration copy, eliminate hero video flicker, and fix mobile product catalog rendering.

## Tasks

### Task 1: Data Models & Rating Endpoints
- [x] Extend `Product` model in `src/models/Product.ts` and `src/types/models.ts` with `rating`, `ratingCount`, `reviewCount`, `createdAt`, and `updatedAt`.
- [x] Create `Rating` model in `src/models/Rating.ts` for visitor/guest ratings tracking.
- [x] Create `/api/products/[slug]/rate` endpoint supporting unauthenticated guest 1-5 star ratings with fingerprinting and atomic aggregate calculation.
- [x] Update `/api/products` endpoint to support `sort` query parameter (`featured`, `popular`, `price_asc`, `price_desc`, `name_asc`, `name_desc`, `newest`, `oldest`).

### Task 2: Storefront UI & Rating Components
- [x] Create `src/components/reviews/QuickStarRating.tsx` for instant guest rating feedback.
- [x] Update `src/components/product/ProductCard.tsx` to display star rating and count.
- [x] Update `src/app/(store)/products/[slug]/page.tsx` with star rating badge, smooth scroll to reviews, and separate guest rating from written reviews.
- [x] Update `src/components/reviews/ProductReviews.tsx` to clearly separate guest star ratings from authenticated written reviews.
- [x] Update `src/app/(store)/collections/page.tsx` with luxury sorting dropdown, mobile collapsible filter drawer, and eager product grid rendering.

### Task 3: Brand Assets & Media Refinements
- [x] Generate custom monochrome luxury perfume imagery for:
  - `public/images/landing_scent_architecture.png`
  - `public/images/landing_scent_anatomy.png`
  - `public/images/about_hero_banner.png`
  - `public/images/hero_video_poster.png`
- [x] Update `src/components/home/HeroSection.tsx` with new poster and smooth fade-in.
- [x] Update `src/components/home/ScentArchitectureSection.tsx` and `ScentAnatomySection.tsx` with new local images.
- [x] Update `src/app/(store)/about/page.tsx` copy (30% -> 40% concentration and 40% Scent Oils) and banner image.

### Task 4: Verification & Regression Testing
- [x] Run full Vitest suite (`npm test`) — 14 test suites, 83 tests passing.
- [x] Run ESLint check (`npm run lint`) — 0 errors, 0 warnings.
- [x] Run Next.js Turbopack build (`npm run build`) — all 25 routes compiled cleanly.
- [x] Verify guest star rating, collections sorting, mobile responsiveness, and image rendering.

## Verification Criteria
- Unauthenticated users can submit star ratings without logging in.
- Authenticated written reviews remain functional and update ratings.
- Product cards and single product page display star ratings and counts.
- Collections page sorts correctly across all 8 sort modes.
- Mobile collections page loads products immediately without blocking scroll threshold.
- Zero unsplash stock image flickering on Hero video.
- About page copy accurately displays 40% concentration with new banner.
- All Vitest tests pass, ESLint is clean (0 errors, 0 warnings), Next.js build succeeds.
